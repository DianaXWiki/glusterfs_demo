syntax = "proto3";

service ShardService {
    rpc SendShardInfo(ShardRequest) returns (ShardResponse);
}

message ShardRequest {
    string shard_path = 1;
    int32 shard_index = 2;
    int32 shard_size = 3;
}

message ShardResponse {
    string status = 1;
}
